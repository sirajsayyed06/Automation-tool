# Python Task Automation Toolkit

A modular automation toolkit written in Python to streamline repetitive daily tasks such as:

- File organization
- Excel/CSV parsing
- Email notifications
- System cleanup

## 🚀 Features
- Config-driven workflows (`settings.json`)
- Logging system (`automation.log`)
- Modular and reusable scripts
- Zero external overhead — pure Python

## 📁 Project Structure
See full folder tree in the repository.
